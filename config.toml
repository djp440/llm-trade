[strategy]
lookback_candles = 192 # 发送给LLM的K线数量
max_open_positions = 3 # 最大同时持仓币种数
risk_per_trade = 0.01 # 单笔风险 (账户权益的 1%)
timeframe = "15m" 

[symbols]
# 启用的交易对列表
active = [
  "SOL/USDT:USDT",
]

[execution]
entry_offset_ticks = 1 # 突破单价格偏移 tick 数
slippage_tolerance = 0.001 # 滑点容忍度
